# pcb_hierplace 0.2.0 交付说明

本版修复只读审查问题，并使用原生适配器完成上传数字电源板的 **103 器件 / 269 物理电气对象 / 47 网络**布局、板框写入、epro2 导出与独立复核。原始工程另存保留。源码与完整复现数据见项目包；详细验证见 `docs/TEST_REPORT.md`。

- 回归测试：**153 项通过**，无失败、错误、跳过。
- 正常固定板框及估算板框示例通过；冲突示例按预期退出 3，未生成正式布局。
- 真实结果：**84.396 × 63.297 mm**；HPWL **1462.180 mm**；内部零违规，独立复核 **64735 项检查通过**。
- 最终交付：`digital_power_board_placed_final.epro2`；完整真实运行副本位于项目的 `sample_runs/real_power_board/`。
- L1 保留 13 个逻辑簇；L2 保留 6 个父功能组。按用户确认采用单一共地 MAIN 电气系统，未将 L2 冒充六个隔离域。
- 总终止原因：`completed`；各阶段实际迭代和未归档细节见验证报告，不能把合法结果等同于完成全部预算。

原生编辑器打开/另存验收仍为 **PENDING**。本次通过所配置的二维布局检查；0.25 mm 铜距作用于器件间异网，封装内同域普通 pad 关系不套此全局值。未完成原生全板 DRC、爬电/安规、实际布线与三维装配验证。

## Ubuntu 使用

解压项目后，在项目根目录执行：

```bash
python3 -m venv .venv
source .venv/bin/activate
python -m pip install -e '.[test]'
```

```bash
python -m pcb_hierplace inspect --project examples/real_power_board/source.epro2
python -m pcb_hierplace validate --config examples/real_power_board/constraints.yaml
python -m pcb_hierplace run --config examples/real_power_board/constraints.yaml --out runs/real_power_board
python -m pcb_hierplace evaluate --run runs/real_power_board
```

独立复核（安装测试依赖后）：

```bash
python scripts/independent_evaluation.py --run runs/real_power_board --epro2 runs/real_power_board/board_placed.epro2 --out runs/real_power_board/independent_evaluation.json
```

配置已经开启自动导出，成功时输出 `runs/real_power_board/board_placed.epro2`。已有输出目录不要覆盖，使用新的目录名。

输入 SHA-256：`4b98e301328db25bb9a1841967888c26dfa1265824b7d7d4046f535118f8b7b7`。

交付 epro2 SHA-256：`ca4ae99419d3ba4f4f4f9a2f2909e3d69c2766b73b7196e23ae467bf12da3efa`。
